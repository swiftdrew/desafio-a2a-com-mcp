[build-system]
requires = ["setuptools==75.8.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ponte-a2a-mcp"
version = "1.0.0"
description = "Agente A2A deterministico com servidor MCP de salas"
requires-python = ">=3.10"
dependencies = [
  "mcp==2.2.0",
  "starlette==1.6.0",
  "uvicorn==0.53.0",
]

[tool.setuptools]
py-modules = []
